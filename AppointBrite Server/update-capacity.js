const mongoose = require('mongoose'); async function updateCapacity() { await mongoose.connect('mongodb://127.0.0.1:27017/appointbrite', { useNewUrlParser: true, useUnifiedTopology: true }); const result = await mongoose.connection.collection('services').updateMany({}, { $set: { capacity: 10 } }); console.log(Updated ${result.modifiedCount} services to have capacity of 10.); process.exit(0); } updateCapacity().catch(console.error);
